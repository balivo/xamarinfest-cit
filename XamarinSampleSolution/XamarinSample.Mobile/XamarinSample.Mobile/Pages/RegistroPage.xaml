<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinSample.Mobile.Pages.RegistroPage">
    <StackLayout>
        <Entry Text="{Binding Usuario}" Placeholder="Usuário" />
        <Entry Text="{Binding Senha}" Placeholder="Senha" IsPassword="True" />
        <Entry Text="{Binding SenhaConfirmar}" Placeholder="Confirmar senha" IsPassword="True" />
        <Button Text="Registrar" Command="{Binding RegistrarCommand}" />
    </StackLayout>
</ContentPage>