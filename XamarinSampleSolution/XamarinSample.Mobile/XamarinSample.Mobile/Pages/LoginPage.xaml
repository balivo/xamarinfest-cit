<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinSample.Mobile.Pages.LoginPage"
             Padding="0, 20, 0, 0">
    <StackLayout>
        <Entry Text="{Binding Usuario}" Placeholder="Usuário" />
        <Entry Text="{Binding Senha}" Placeholder="Senha" IsPassword="True" />
        <Button Text="Login" Command="{Binding LoginCommand}" />
        <Button Text="Novo Usuário" Command="{Binding RegisterCommand}" />
    </StackLayout>
</ContentPage>